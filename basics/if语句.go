package main

/*
// 一： 示例1
func main() {
	const filename = "D:/DodeProject/basics/text.txt"
	neirong, err := os.ReadFile(filename)
	if err != nil {
		fmt.Println(err)
	} else {
		fmt.Printf("%s\n", neirong) // %s 是占位符，表示要填充一个字符串类型的变量。这里用来填充文件里的neirong
	}
}

*/

// 二： 示例2
/*
func main() {
	const filename = "D:/DodeProject/basics/text.txt"
	if contents, err := os.ReadFile(filename); err != nil {
		fmt.Println(err)
	} else {
		fmt.Printf("%s\n", contents)
	}
}

*/
